// Assume the assets are located in the 'assets' directory
const assetsPath = '../';

//----------------------- ONBOARDING and AUTH USED IMAGES ---------------------------------
export const logoImage = require(`${assetsPath}images/logo.png`);
